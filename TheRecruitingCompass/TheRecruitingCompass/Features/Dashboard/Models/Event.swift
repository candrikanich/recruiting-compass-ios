import Foundation

/// Lightweight alias: Dashboard widgets use `FullEvent` from the Events feature directly.
/// The canonical model is `FullEvent` (Features/Events/Models/FullEvent.swift).
typealias Event = FullEvent
