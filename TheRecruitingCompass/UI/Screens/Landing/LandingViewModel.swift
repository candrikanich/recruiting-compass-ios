import Foundation

/// Landing screen ViewModel
/// No data fetching needed - purely UI-driven navigation
@MainActor
final class LandingViewModel: ObservableObject {
    // No state needed - this is a purely navigational screen
    // In a real app, you might check if user is already logged in
}
